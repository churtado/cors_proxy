{
  "name": "cors_proxy",
  "version": "1.0.0",
  "description": "cors proxy server",
  "main": "cors_proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4",
    "request": "^2.81.0"
  }
}
